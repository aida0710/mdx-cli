# MDX 1 CLI ツール 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** MDX 1 クラウドインフラプラットフォームのAPIをコマンドラインから操作するCLIツールを構築する

**Architecture:** Typer + Pydantic v2 のレイヤード構造。commands層（CLIインターフェース）→ api層（HTTPクライアント+エンドポイント）→ models層（Pydanticモデル）の3層。認証はhttpx.Authサブクラスで透過的にJWT管理し、クレデンシャルはkeyring + Fernetフォールバックで保存する。

**Tech Stack:** Python 3.13, uv, Typer, httpx, Pydantic v2, pydantic-settings, Rich, keyring, cryptography, pytest

**Spec:** `docs/superpowers/specs/2026-04-14-mdx-cli-design.md`

---

## ファイルマップ

| ファイル | 責務 | タスク |
|---------|------|-------|
| `pyproject.toml` | 依存関係・エントリポイント定義 | 1 |
| `src/mdx_cli/__init__.py` | パッケージ初期化 | 1 |
| `src/mdx_cli/settings.py` | pydantic-settings設定クラス | 2 |
| `src/mdx_cli/models/enums.py` | VMStatus, TaskStatus, ServiceLevel等 | 3 |
| `src/mdx_cli/models/__init__.py` | モデルパッケージ | 3 |
| `src/mdx_cli/models/auth.py` | TokenPairモデル | 3 |
| `src/mdx_cli/models/project.py` | Projectモデル | 3 |
| `src/mdx_cli/models/vm.py` | VM, VMDeployRequest, VMDeployResponseモデル | 3 |
| `src/mdx_cli/models/network.py` | Segment, ACL, DNATモデル | 3 |
| `src/mdx_cli/models/task.py` | Taskモデル（is_terminal computed field） | 3 |
| `src/mdx_cli/output/__init__.py` | 出力パッケージ | 4 |
| `src/mdx_cli/output/tables.py` | モデルごとのカラム定義 | 4 |
| `src/mdx_cli/output/formatting.py` | Richテーブル/JSON出力切替 | 4 |
| `src/mdx_cli/credentials/__init__.py` | クレデンシャルパッケージ | 5 |
| `src/mdx_cli/credentials/store.py` | keyring + Fernet暗号化保存 | 5 |
| `src/mdx_cli/api/__init__.py` | APIパッケージ | 6 |
| `src/mdx_cli/api/client.py` | httpxクライアントファクトリ | 6 |
| `src/mdx_cli/api/auth.py` | MDXAuth（httpx.Authサブクラス） | 6 |
| `src/mdx_cli/api/endpoints/__init__.py` | エンドポイントパッケージ | 7-10 |
| `src/mdx_cli/api/endpoints/projects.py` | プロジェクトAPI | 7 |
| `src/mdx_cli/api/endpoints/vms.py` | VM API | 8 |
| `src/mdx_cli/api/endpoints/networks.py` | ネットワークAPI | 9 |
| `src/mdx_cli/api/endpoints/tasks.py` | タスクAPI（ポーリング含む） | 10 |
| `src/mdx_cli/api/endpoints/templates.py` | テンプレートAPI | 14 |
| `src/mdx_cli/commands/__init__.py` | コマンドパッケージ | 11 |
| `src/mdx_cli/commands/auth.py` | login/logout/statusコマンド | 11 |
| `src/mdx_cli/commands/project.py` | projectサブコマンド | 12 |
| `src/mdx_cli/commands/vm.py` | vmサブコマンド | 13 |
| `src/mdx_cli/commands/network.py` | networkサブコマンド | 14 |
| `src/mdx_cli/commands/template.py` | templateサブコマンド | 14 |
| `src/mdx_cli/commands/task.py` | taskサブコマンド | 15 |
| `src/mdx_cli/main.py` | Typerルートアプリ、グローバルオプション | 16 |

---

### Task 1: プロジェクト初期化と依存関係

**Files:**
- Modify: `pyproject.toml`
- Create: `src/mdx_cli/__init__.py`
- Create: `tests/__init__.py`
- Create: `tests/conftest.py`

- [ ] **Step 1: pyproject.toml に依存関係とエントリポイントを追加**

```toml
[project]
name = "mdx-cli"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "typer[all]>=0.12",
    "httpx>=0.27",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "keyring>=25.0",
    "cryptography>=42.0",
]

[project.scripts]
mdx = "mdx_cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mdx_cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.14",
    "respx>=0.22",
]
```

- [ ] **Step 2: パッケージ初期化ファイルを作成**

`src/mdx_cli/__init__.py`:
```python
"""MDX 1 CLI ツール"""
```

`tests/__init__.py`:
```python
```

`tests/conftest.py`:
```python
"""共通テストフィクスチャ"""
```

- [ ] **Step 3: 依存関係をインストール**

Run: `cd /Users/aida/projects/mdx-cli && uv sync`
Expected: 全依存パッケージがインストールされる

- [ ] **Step 4: インストールを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run python -c "import typer; import httpx; import pydantic; print('OK')"`
Expected: `OK`

- [ ] **Step 5: コミット**

```bash
git init
git add pyproject.toml uv.lock src/mdx_cli/__init__.py tests/__init__.py tests/conftest.py
git commit -m "feat: プロジェクト初期化、依存関係追加"
```

---

### Task 2: 設定管理（Settings）

**Files:**
- Create: `src/mdx_cli/settings.py`
- Create: `tests/test_settings.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_settings.py`:
```python
from mdx_cli.settings import Settings


def test_default_base_url():
    settings = Settings()
    assert settings.base_url == "https://oprpl.mdx.jp"


def test_default_request_timeout():
    settings = Settings()
    assert settings.request_timeout == 30


def test_default_task_poll_interval():
    settings = Settings()
    assert settings.task_poll_interval == 3


def test_default_task_poll_timeout():
    settings = Settings()
    assert settings.task_poll_timeout == 600


def test_default_project_id_is_none():
    settings = Settings()
    assert settings.default_project_id is None


def test_env_override(monkeypatch):
    monkeypatch.setenv("MDX_BASE_URL", "https://test.example.com")
    monkeypatch.setenv("MDX_DEFAULT_PROJECT_ID", "test-project-123")
    settings = Settings()
    assert settings.base_url == "https://test.example.com"
    assert settings.default_project_id == "test-project-123"
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_settings.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'mdx_cli.settings'`

- [ ] **Step 3: 最小限の実装を書く**

`src/mdx_cli/settings.py`:
```python
from pathlib import Path

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    base_url: str = "https://oprpl.mdx.jp"
    default_project_id: str | None = None
    request_timeout: int = 30
    task_poll_interval: int = 3
    task_poll_timeout: int = 600
    config_dir: Path = Path.home() / ".config" / "mdx-cli"

    model_config = SettingsConfigDict(
        env_prefix="MDX_",
    )
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_settings.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/settings.py tests/test_settings.py
git commit -m "feat: pydantic-settings設定クラスを追加"
```

---

### Task 3: Pydanticモデル

**Files:**
- Create: `src/mdx_cli/models/__init__.py`
- Create: `src/mdx_cli/models/enums.py`
- Create: `src/mdx_cli/models/auth.py`
- Create: `src/mdx_cli/models/project.py`
- Create: `src/mdx_cli/models/vm.py`
- Create: `src/mdx_cli/models/network.py`
- Create: `src/mdx_cli/models/task.py`
- Create: `tests/test_models/__init__.py`
- Create: `tests/test_models/test_enums.py`
- Create: `tests/test_models/test_vm.py`
- Create: `tests/test_models/test_task.py`

- [ ] **Step 1: 列挙型の失敗するテストを書く**

`tests/test_models/__init__.py`:
```python
```

`tests/test_models/test_enums.py`:
```python
from mdx_cli.models.enums import ServiceLevel, TaskStatus, VMStatus


def test_vm_status_running():
    assert VMStatus.RUNNING == "Running"


def test_vm_status_stopped():
    assert VMStatus.STOPPED == "Stopped"


def test_service_level_spot():
    assert ServiceLevel.SPOT == "spot"


def test_service_level_guarantee():
    assert ServiceLevel.GUARANTEE == "guarantee"


def test_task_status_values():
    assert TaskStatus.RUNNING == "Running"
    assert TaskStatus.COMPLETED == "Completed"
    assert TaskStatus.FAILED == "Failed"
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_models/test_enums.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 列挙型を実装**

`src/mdx_cli/models/__init__.py`:
```python
"""MDX APIモデル"""
```

`src/mdx_cli/models/enums.py`:
```python
from enum import Enum


class VMStatus(str, Enum):
    RUNNING = "Running"
    STOPPED = "Stopped"
    DEPLOYING = "Deploying"
    DESTROYING = "Destroying"


class ServiceLevel(str, Enum):
    SPOT = "spot"
    GUARANTEE = "guarantee"


class TaskStatus(str, Enum):
    RUNNING = "Running"
    COMPLETED = "Completed"
    FAILED = "Failed"
```

- [ ] **Step 4: 列挙型テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_models/test_enums.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: VMモデルの失敗するテストを書く**

`tests/test_models/test_vm.py`:
```python
import pytest
from pydantic import ValidationError

from mdx_cli.models.vm import VM, VMDeployRequest, VMDeployResponse


def test_vm_from_dict():
    data = {
        "uuid": "abc-123",
        "name": "test-vm",
        "status": "Running",
        "project": "proj-456",
        "pack_type": "cpu",
        "pack_num": 3,
        "service_level": "spot",
    }
    vm = VM.model_validate(data)
    assert vm.uuid == "abc-123"
    assert vm.name == "test-vm"
    assert vm.status.value == "Running"
    assert vm.pack_num == 3


def test_vm_invalid_status():
    data = {
        "uuid": "abc-123",
        "name": "test-vm",
        "status": "InvalidStatus",
        "project": "proj-456",
        "pack_type": "cpu",
        "pack_num": 3,
        "service_level": "spot",
    }
    with pytest.raises(ValidationError):
        VM.model_validate(data)


def test_vm_deploy_request_defaults():
    req = VMDeployRequest(
        catalog="cat-1",
        project="proj-1",
        vm_name="my-vm",
        network_adapters=[{"adapter_number": 1, "segment": "seg-1"}],
        shared_key="ssh-rsa AAAA...",
        template_name="ubuntu-22",
    )
    assert req.disk_size == 40
    assert req.pack_type == "cpu"
    assert req.pack_num == 3
    assert req.service_level.value == "spot"
    assert req.power_on is False
    assert req.nvlink is False


def test_vm_deploy_response():
    resp = VMDeployResponse.model_validate({"task_id": ["task-abc"]})
    assert resp.task_id == ["task-abc"]
```

- [ ] **Step 6: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_models/test_vm.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 7: VMモデルを実装**

`src/mdx_cli/models/vm.py`:
```python
from pydantic import BaseModel

from mdx_cli.models.enums import ServiceLevel, VMStatus


class VM(BaseModel):
    uuid: str
    name: str
    status: VMStatus
    project: str
    pack_type: str
    pack_num: int
    service_level: ServiceLevel


class VMDeployRequest(BaseModel):
    catalog: str
    project: str
    vm_name: str
    disk_size: int = 40
    storage_network: str = "portgroup"
    pack_type: str = "cpu"
    pack_num: int = 3
    service_level: ServiceLevel = ServiceLevel.SPOT
    network_adapters: list[dict]
    shared_key: str
    power_on: bool = False
    os_type: str = "Linux"
    template_name: str
    nvlink: bool = False


class VMDeployResponse(BaseModel):
    task_id: list[str]
```

- [ ] **Step 8: VMモデルテストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_models/test_vm.py -v`
Expected: 全テスト PASS

- [ ] **Step 9: Taskモデルの失敗するテストを書く**

`tests/test_models/test_task.py`:
```python
from mdx_cli.models.task import Task


def test_task_from_dict():
    data = {
        "uuid": "task-123",
        "type": "デプロイ",
        "object_uuid": "vm-456",
        "object_name": "test-vm",
        "start_datetime": "2026-04-14 16:34:13",
        "end_datetime": None,
        "status": "Running",
        "progress": 63,
    }
    task = Task.model_validate(data)
    assert task.uuid == "task-123"
    assert task.progress == 63
    assert task.is_terminal is False


def test_task_completed_is_terminal():
    data = {
        "uuid": "task-123",
        "type": "デプロイ",
        "object_uuid": "vm-456",
        "object_name": "test-vm",
        "start_datetime": "2026-04-14 16:34:13",
        "end_datetime": "2026-04-14 16:40:00",
        "status": "Completed",
        "progress": 100,
    }
    task = Task.model_validate(data)
    assert task.is_terminal is True


def test_task_failed_is_terminal():
    data = {
        "uuid": "task-123",
        "type": "デプロイ",
        "object_uuid": "vm-456",
        "object_name": "test-vm",
        "start_datetime": "2026-04-14 16:34:13",
        "end_datetime": "2026-04-14 16:40:00",
        "status": "Failed",
        "progress": 50,
    }
    task = Task.model_validate(data)
    assert task.is_terminal is True
```

- [ ] **Step 10: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_models/test_task.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 11: Taskモデルを実装**

`src/mdx_cli/models/task.py`:
```python
from pydantic import BaseModel, computed_field

from mdx_cli.models.enums import TaskStatus


class Task(BaseModel):
    uuid: str
    type: str
    object_uuid: str
    object_name: str
    start_datetime: str
    end_datetime: str | None
    status: TaskStatus
    progress: int

    @computed_field
    @property
    def is_terminal(self) -> bool:
        return self.status in (TaskStatus.COMPLETED, TaskStatus.FAILED)
```

- [ ] **Step 12: Taskモデルテストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_models/test_task.py -v`
Expected: 全テスト PASS

- [ ] **Step 13: 残りのモデル（auth, project, network）を実装**

`src/mdx_cli/models/auth.py`:
```python
from datetime import datetime

from pydantic import BaseModel


class TokenPair(BaseModel):
    token: str
    expires_at: datetime | None = None
```

`src/mdx_cli/models/project.py`:
```python
from pydantic import BaseModel


class Project(BaseModel):
    uuid: str
    name: str
    description: str = ""


class ProjectSummary(BaseModel):
    uuid: str
    name: str
    description: str = ""


class StorageInfo(BaseModel):
    uuid: str
    name: str = ""


class AccessKey(BaseModel):
    uuid: str
    name: str = ""
```

`src/mdx_cli/models/network.py`:
```python
from pydantic import BaseModel


class Segment(BaseModel):
    uuid: str
    name: str


class SegmentSummary(BaseModel):
    uuid: str
    name: str


class ACL(BaseModel):
    uuid: str


class DNAT(BaseModel):
    uuid: str
```

注意: project.py と network.py のフィールドは実際のAPIレスポンスに合わせて後で調整する。`model_config = ConfigDict(extra="allow")` を各モデルに追加して未知フィールドを許容することも検討する。

- [ ] **Step 14: 全モデルテストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_models/ -v`
Expected: 全テスト PASS

- [ ] **Step 15: コミット**

```bash
git add src/mdx_cli/models/ tests/test_models/
git commit -m "feat: Pydanticモデル（enums, vm, task, auth, project, network）を追加"
```

---

### Task 4: 出力フォーマット（Richテーブル / JSON切替）

**Files:**
- Create: `src/mdx_cli/output/__init__.py`
- Create: `src/mdx_cli/output/tables.py`
- Create: `src/mdx_cli/output/formatting.py`
- Create: `tests/test_output/__init__.py`
- Create: `tests/test_output/test_formatting.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_output/__init__.py`:
```python
```

`tests/test_output/test_formatting.py`:
```python
import json

from mdx_cli.models.vm import VM
from mdx_cli.output.formatting import render_json, render_table
from mdx_cli.output.tables import VM_COLUMNS


def _make_vm() -> VM:
    return VM(
        uuid="abc-123",
        name="test-vm",
        status="Running",
        project="proj-456",
        pack_type="cpu",
        pack_num=3,
        service_level="spot",
    )


def test_render_json_single(capsys):
    vm = _make_vm()
    render_json(vm)
    captured = capsys.readouterr()
    data = json.loads(captured.out)
    assert data["uuid"] == "abc-123"
    assert data["name"] == "test-vm"


def test_render_json_list(capsys):
    vms = [_make_vm(), _make_vm()]
    render_json(vms)
    captured = capsys.readouterr()
    data = json.loads(captured.out)
    assert len(data) == 2


def test_render_table(capsys):
    vm = _make_vm()
    render_table([vm], VM_COLUMNS)
    captured = capsys.readouterr()
    assert "abc-123" in captured.out
    assert "test-vm" in captured.out
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_output/test_formatting.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: テーブルカラム定義を実装**

`src/mdx_cli/output/__init__.py`:
```python
"""出力フォーマット"""
```

`src/mdx_cli/output/tables.py`:
```python
"""モデルごとのテーブルカラム定義

各定義は (表示名, モデルフィールド名) のタプルのリスト。
"""

VM_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
    ("名前", "name"),
    ("状態", "status"),
    ("パック数", "pack_num"),
    ("サービスレベル", "service_level"),
]

PROJECT_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
    ("名前", "name"),
    ("説明", "description"),
]

SEGMENT_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
    ("名前", "name"),
]

TASK_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
    ("タイプ", "type"),
    ("対象", "object_name"),
    ("状態", "status"),
    ("進捗", "progress"),
]

DNAT_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
]

ACL_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
]

TEMPLATE_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
]

ACCESS_KEY_COLUMNS: list[tuple[str, str]] = [
    ("UUID", "uuid"),
    ("名前", "name"),
]
```

- [ ] **Step 4: フォーマッターを実装**

`src/mdx_cli/output/formatting.py`:
```python
import json

from pydantic import BaseModel
from rich.console import Console
from rich.table import Table

console = Console()


def render_json(data: BaseModel | list[BaseModel]) -> None:
    if isinstance(data, list):
        output = [item.model_dump(mode="json") for item in data]
    else:
        output = data.model_dump(mode="json")
    print(json.dumps(output, indent=2, ensure_ascii=False))


def render_table(data: list[BaseModel], columns: list[tuple[str, str]]) -> None:
    table = Table()
    for header, _ in columns:
        table.add_column(header)
    for item in data:
        row = []
        for _, field in columns:
            value = getattr(item, field, "")
            row.append(str(value))
        table.add_row(*row)
    console.print(table)


def render(
    data: BaseModel | list[BaseModel],
    columns: list[tuple[str, str]],
    json_mode: bool,
) -> None:
    if json_mode:
        render_json(data)
    else:
        items = data if isinstance(data, list) else [data]
        render_table(items, columns)
```

- [ ] **Step 5: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_output/test_formatting.py -v`
Expected: 全テスト PASS

- [ ] **Step 6: コミット**

```bash
git add src/mdx_cli/output/ tests/test_output/
git commit -m "feat: Richテーブル/JSON出力フォーマッターを追加"
```

---

### Task 5: クレデンシャル保存（keyring + Fernetフォールバック）

**Files:**
- Create: `src/mdx_cli/credentials/__init__.py`
- Create: `src/mdx_cli/credentials/store.py`
- Create: `tests/test_credentials/__init__.py`
- Create: `tests/test_credentials/test_store.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_credentials/__init__.py`:
```python
```

`tests/test_credentials/test_store.py`:
```python
import json
from pathlib import Path

from mdx_cli.credentials.store import CredentialStore


def test_save_and_load_credentials(tmp_path, mocker):
    """keyringが使えない場合、Fernetフォールバックで保存・読込できる"""
    mocker.patch("mdx_cli.credentials.store.keyring_available", return_value=False)
    store = CredentialStore(config_dir=tmp_path)
    store.save_credentials("testuser", "testpass")
    username, password = store.load_credentials()
    assert username == "testuser"
    assert password == "testpass"


def test_delete_credentials(tmp_path, mocker):
    mocker.patch("mdx_cli.credentials.store.keyring_available", return_value=False)
    store = CredentialStore(config_dir=tmp_path)
    store.save_credentials("testuser", "testpass")
    store.delete_credentials()
    result = store.load_credentials()
    assert result is None


def test_save_and_load_token(tmp_path):
    store = CredentialStore(config_dir=tmp_path)
    store.save_token("jwt-token-abc")
    token = store.load_token()
    assert token == "jwt-token-abc"


def test_delete_token(tmp_path):
    store = CredentialStore(config_dir=tmp_path)
    store.save_token("jwt-token-abc")
    store.delete_token()
    token = store.load_token()
    assert token is None


def test_load_credentials_when_none_saved(tmp_path, mocker):
    mocker.patch("mdx_cli.credentials.store.keyring_available", return_value=False)
    store = CredentialStore(config_dir=tmp_path)
    result = store.load_credentials()
    assert result is None
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_credentials/test_store.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: クレデンシャルストアを実装**

`src/mdx_cli/credentials/__init__.py`:
```python
"""クレデンシャル管理"""
```

`src/mdx_cli/credentials/store.py`:
```python
import json
import platform
from pathlib import Path

from cryptography.fernet import Fernet


SERVICE_NAME = "mdx-cli"


def keyring_available() -> bool:
    try:
        import keyring
        keyring.get_password(SERVICE_NAME, "__test__")
        return True
    except Exception:
        return False


def _derive_key(config_dir: Path) -> bytes:
    """マシン固有のキーを導出する（Fernetフォールバック用）"""
    import hashlib
    import base64

    machine_id = f"{platform.node()}-{platform.machine()}-mdx-cli-salt"
    key = hashlib.sha256(machine_id.encode()).digest()
    return base64.urlsafe_b64encode(key)


class CredentialStore:
    def __init__(self, config_dir: Path | None = None):
        if config_dir is None:
            config_dir = Path.home() / ".config" / "mdx-cli"
        self._config_dir = config_dir
        self._config_dir.mkdir(parents=True, exist_ok=True)

    def save_credentials(self, username: str, password: str) -> None:
        if keyring_available():
            import keyring
            keyring.set_password(SERVICE_NAME, "username", username)
            keyring.set_password(SERVICE_NAME, "password", password)
        else:
            self._save_credentials_fernet(username, password)

    def load_credentials(self) -> tuple[str, str] | None:
        if keyring_available():
            import keyring
            username = keyring.get_password(SERVICE_NAME, "username")
            password = keyring.get_password(SERVICE_NAME, "password")
            if username and password:
                return (username, password)
            return None
        else:
            return self._load_credentials_fernet()

    def delete_credentials(self) -> None:
        if keyring_available():
            import keyring
            try:
                keyring.delete_password(SERVICE_NAME, "username")
                keyring.delete_password(SERVICE_NAME, "password")
            except keyring.errors.PasswordDeleteError:
                pass
        else:
            cred_file = self._config_dir / "credentials.enc"
            if cred_file.exists():
                cred_file.unlink()

    def save_token(self, token: str) -> None:
        token_file = self._config_dir / "token.json"
        token_file.write_text(json.dumps({"token": token}))

    def load_token(self) -> str | None:
        token_file = self._config_dir / "token.json"
        if not token_file.exists():
            return None
        data = json.loads(token_file.read_text())
        return data.get("token")

    def delete_token(self) -> None:
        token_file = self._config_dir / "token.json"
        if token_file.exists():
            token_file.unlink()

    def _save_credentials_fernet(self, username: str, password: str) -> None:
        key = _derive_key(self._config_dir)
        f = Fernet(key)
        data = json.dumps({"username": username, "password": password}).encode()
        encrypted = f.encrypt(data)
        cred_file = self._config_dir / "credentials.enc"
        cred_file.write_bytes(encrypted)

    def _load_credentials_fernet(self) -> tuple[str, str] | None:
        cred_file = self._config_dir / "credentials.enc"
        if not cred_file.exists():
            return None
        key = _derive_key(self._config_dir)
        f = Fernet(key)
        decrypted = f.decrypt(cred_file.read_bytes())
        data = json.loads(decrypted)
        return (data["username"], data["password"])
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_credentials/test_store.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/credentials/ tests/test_credentials/
git commit -m "feat: keyring + Fernetフォールバックのクレデンシャルストアを追加"
```

---

### Task 6: APIクライアントとJWT認証

**Files:**
- Create: `src/mdx_cli/api/__init__.py`
- Create: `src/mdx_cli/api/client.py`
- Create: `src/mdx_cli/api/auth.py`
- Create: `tests/test_api/__init__.py`
- Create: `tests/test_api/test_auth.py`
- Create: `tests/test_api/test_client.py`

- [ ] **Step 1: JWT認証の失敗するテストを書く**

`tests/test_api/__init__.py`:
```python
```

`tests/test_api/test_auth.py`:
```python
import httpx
import respx

from mdx_cli.api.auth import MDXAuth


def test_auth_injects_jwt_header():
    """保存済みトークンがあればAuthorizationヘッダーに注入される"""
    auth = MDXAuth(token="test-jwt-token")
    request = httpx.Request("GET", "https://oprpl.mdx.jp/api/project/assigned/")
    flow = auth.auth_flow(request)
    modified_request = next(flow)
    assert modified_request.headers["Authorization"] == "JWT test-jwt-token"


def test_auth_no_token():
    """トークンがなければヘッダーは付与されない"""
    auth = MDXAuth(token=None)
    request = httpx.Request("GET", "https://oprpl.mdx.jp/api/project/assigned/")
    flow = auth.auth_flow(request)
    modified_request = next(flow)
    assert "Authorization" not in modified_request.headers
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_auth.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: MDXAuth を実装**

`src/mdx_cli/api/__init__.py`:
```python
"""MDX APIクライアント"""
```

`src/mdx_cli/api/auth.py`:
```python
from typing import Generator

import httpx


class MDXAuth(httpx.Auth):
    def __init__(self, token: str | None = None):
        self.token = token

    def auth_flow(
        self, request: httpx.Request
    ) -> Generator[httpx.Request, httpx.Response, None]:
        if self.token:
            request.headers["Authorization"] = f"JWT {self.token}"
        yield request
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_auth.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: クライアントファクトリの失敗するテストを書く**

`tests/test_api/test_client.py`:
```python
from mdx_cli.api.client import create_client


def test_create_client_default_base_url():
    client = create_client()
    assert str(client.base_url) == "https://oprpl.mdx.jp/"


def test_create_client_custom_base_url():
    client = create_client(base_url="https://test.example.com")
    assert str(client.base_url) == "https://test.example.com/"


def test_create_client_with_token():
    client = create_client(token="my-jwt")
    assert client.auth is not None
```

- [ ] **Step 6: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_client.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 7: クライアントファクトリを実装**

`src/mdx_cli/api/client.py`:
```python
import httpx

from mdx_cli.api.auth import MDXAuth
from mdx_cli.settings import Settings


def create_client(
    base_url: str | None = None,
    token: str | None = None,
    timeout: int | None = None,
) -> httpx.Client:
    settings = Settings()
    return httpx.Client(
        base_url=base_url or settings.base_url,
        timeout=timeout or settings.request_timeout,
        auth=MDXAuth(token=token) if token else None,
    )
```

- [ ] **Step 8: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_client.py -v`
Expected: 全テスト PASS

- [ ] **Step 9: コミット**

```bash
git add src/mdx_cli/api/__init__.py src/mdx_cli/api/auth.py src/mdx_cli/api/client.py tests/test_api/
git commit -m "feat: httpxクライアントとJWT認証（MDXAuth）を追加"
```

---

### Task 7: プロジェクトAPIエンドポイント

**Files:**
- Create: `src/mdx_cli/api/endpoints/__init__.py`
- Create: `src/mdx_cli/api/endpoints/projects.py`
- Create: `tests/test_api/test_endpoints/__init__.py`
- Create: `tests/test_api/test_endpoints/test_projects.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_api/test_endpoints/__init__.py`:
```python
```

`tests/test_api/test_endpoints/test_projects.py`:
```python
import httpx
import respx

from mdx_cli.api.endpoints.projects import list_projects, get_project_summary


@respx.mock
def test_list_projects():
    respx.get("/api/project/assigned/").mock(
        return_value=httpx.Response(
            200,
            json={
                "results": [
                    {"uuid": "proj-1", "name": "Project 1", "description": "desc1"},
                    {"uuid": "proj-2", "name": "Project 2", "description": "desc2"},
                ]
            },
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    projects = list_projects(client)
    assert len(projects) == 2
    assert projects[0].uuid == "proj-1"
    assert projects[1].name == "Project 2"


@respx.mock
def test_get_project_summary():
    respx.get("/api/project/proj-1/summary/").mock(
        return_value=httpx.Response(
            200,
            json={"uuid": "proj-1", "name": "Project 1", "description": "Summary"},
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    summary = get_project_summary(client, "proj-1")
    assert summary.uuid == "proj-1"
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_projects.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: プロジェクトエンドポイントを実装**

`src/mdx_cli/api/endpoints/__init__.py`:
```python
"""APIエンドポイント"""
```

`src/mdx_cli/api/endpoints/projects.py`:
```python
import httpx

from mdx_cli.models.project import AccessKey, Project, ProjectSummary, StorageInfo


def list_projects(client: httpx.Client) -> list[Project]:
    resp = client.get("/api/project/assigned/", params={"page_size": 1000})
    resp.raise_for_status()
    return [Project.model_validate(item) for item in resp.json()["results"]]


def get_project_summary(client: httpx.Client, project_id: str) -> ProjectSummary:
    resp = client.get(f"/api/project/{project_id}/summary/")
    resp.raise_for_status()
    return ProjectSummary.model_validate(resp.json())


def get_project_storage(client: httpx.Client, project_id: str) -> StorageInfo:
    resp = client.get(f"/api/project/{project_id}/storage/")
    resp.raise_for_status()
    return StorageInfo.model_validate(resp.json())


def list_access_keys(client: httpx.Client, project_id: str) -> list[AccessKey]:
    resp = client.get(
        f"/api/project/{project_id}/access_key/", params={"page_size": 1000}
    )
    resp.raise_for_status()
    return [AccessKey.model_validate(item) for item in resp.json()["results"]]
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_projects.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/api/endpoints/ tests/test_api/test_endpoints/
git commit -m "feat: プロジェクトAPIエンドポイントを追加"
```

---

### Task 8: VM APIエンドポイント

**Files:**
- Create: `src/mdx_cli/api/endpoints/vms.py`
- Create: `tests/test_api/test_endpoints/test_vms.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_api/test_endpoints/test_vms.py`:
```python
import httpx
import respx

from mdx_cli.api.endpoints.vms import list_vms, get_vm, deploy_vm, power_on_vm, power_off_vm, destroy_vm
from mdx_cli.models.vm import VMDeployRequest


@respx.mock
def test_list_vms():
    respx.get("/api/vm/project/proj-1/").mock(
        return_value=httpx.Response(
            200,
            json={
                "results": [
                    {
                        "uuid": "vm-1",
                        "name": "test-vm",
                        "status": "Running",
                        "project": "proj-1",
                        "pack_type": "cpu",
                        "pack_num": 3,
                        "service_level": "spot",
                    }
                ]
            },
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    vms = list_vms(client, "proj-1")
    assert len(vms) == 1
    assert vms[0].uuid == "vm-1"


@respx.mock
def test_get_vm():
    respx.get("/api/vm/vm-1/").mock(
        return_value=httpx.Response(
            200,
            json={
                "uuid": "vm-1",
                "name": "test-vm",
                "status": "Running",
                "project": "proj-1",
                "pack_type": "cpu",
                "pack_num": 3,
                "service_level": "spot",
            },
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    vm = get_vm(client, "vm-1")
    assert vm.name == "test-vm"


@respx.mock
def test_deploy_vm():
    respx.post("/api/vm/deploy/").mock(
        return_value=httpx.Response(200, json={"task_id": ["task-abc"]})
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    req = VMDeployRequest(
        catalog="cat-1",
        project="proj-1",
        vm_name="my-vm",
        network_adapters=[{"adapter_number": 1, "segment": "seg-1"}],
        shared_key="ssh-rsa AAAA...",
        template_name="ubuntu-22",
    )
    resp = deploy_vm(client, req)
    assert resp.task_id == ["task-abc"]


@respx.mock
def test_power_on_vm():
    respx.post("/api/vm/vm-1/power_on/").mock(
        return_value=httpx.Response(200, json={})
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    power_on_vm(client, "vm-1", "spot")


@respx.mock
def test_power_off_vm():
    respx.post("/api/vm/vm-1/power_off/").mock(
        return_value=httpx.Response(200, json={})
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    power_off_vm(client, "vm-1")


@respx.mock
def test_destroy_vm():
    respx.post("/api/vm/vm-1/destroy/").mock(
        return_value=httpx.Response(200, json={"task_id": ["task-del"]})
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    resp = destroy_vm(client, "vm-1")
    assert resp.task_id == ["task-del"]
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_vms.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: VM APIエンドポイントを実装**

`src/mdx_cli/api/endpoints/vms.py`:
```python
import httpx

from mdx_cli.models.vm import VM, VMDeployRequest, VMDeployResponse


def list_vms(client: httpx.Client, project_id: str) -> list[VM]:
    resp = client.get(f"/api/vm/project/{project_id}/", params={"page_size": 1000})
    resp.raise_for_status()
    return [VM.model_validate(item) for item in resp.json()["results"]]


def get_vm(client: httpx.Client, vm_id: str) -> VM:
    resp = client.get(f"/api/vm/{vm_id}/")
    resp.raise_for_status()
    return VM.model_validate(resp.json())


def deploy_vm(client: httpx.Client, request: VMDeployRequest) -> VMDeployResponse:
    resp = client.post("/api/vm/deploy/", json=request.model_dump())
    resp.raise_for_status()
    return VMDeployResponse.model_validate(resp.json())


def power_on_vm(client: httpx.Client, vm_id: str, service_level: str = "spot") -> None:
    resp = client.post(
        f"/api/vm/{vm_id}/power_on/", json={"service_level": service_level}
    )
    resp.raise_for_status()


def power_off_vm(client: httpx.Client, vm_id: str) -> None:
    resp = client.post(f"/api/vm/{vm_id}/power_off/")
    resp.raise_for_status()


def destroy_vm(client: httpx.Client, vm_id: str) -> VMDeployResponse:
    resp = client.post(f"/api/vm/{vm_id}/destroy/")
    resp.raise_for_status()
    return VMDeployResponse.model_validate(resp.json())


def sync_vms(client: httpx.Client, project_id: str) -> None:
    resp = client.post(f"/api/vm/synchronize/project/{project_id}/")
    resp.raise_for_status()
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_vms.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/api/endpoints/vms.py tests/test_api/test_endpoints/test_vms.py
git commit -m "feat: VM APIエンドポイント（list, get, deploy, power, destroy, sync）を追加"
```

---

### Task 9: ネットワークAPIエンドポイント

**Files:**
- Create: `src/mdx_cli/api/endpoints/networks.py`
- Create: `tests/test_api/test_endpoints/test_networks.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_api/test_endpoints/test_networks.py`:
```python
import httpx
import respx

from mdx_cli.api.endpoints.networks import (
    list_segments,
    get_segment_summary,
    list_acls,
    list_dnats,
)


@respx.mock
def test_list_segments():
    respx.get("/api/segment/project/proj-1/all/").mock(
        return_value=httpx.Response(
            200, json=[{"uuid": "seg-1", "name": "Segment 1"}]
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    segments = list_segments(client, "proj-1")
    assert len(segments) == 1
    assert segments[0].uuid == "seg-1"


@respx.mock
def test_get_segment_summary():
    respx.get("/api/segment/seg-1/summary/").mock(
        return_value=httpx.Response(
            200, json={"uuid": "seg-1", "name": "Segment 1"}
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    summary = get_segment_summary(client, "seg-1")
    assert summary.uuid == "seg-1"


@respx.mock
def test_list_acls():
    respx.get("/api/acl/segment/seg-1/").mock(
        return_value=httpx.Response(
            200, json={"results": [{"uuid": "acl-1"}]}
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    acls = list_acls(client, "seg-1")
    assert len(acls) == 1


@respx.mock
def test_list_dnats():
    respx.get("/api/dnat/project/proj-1/").mock(
        return_value=httpx.Response(
            200, json={"results": [{"uuid": "dnat-1"}]}
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    dnats = list_dnats(client, "proj-1")
    assert len(dnats) == 1
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_networks.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: ネットワークエンドポイントを実装**

`src/mdx_cli/api/endpoints/networks.py`:
```python
import httpx

from mdx_cli.models.network import ACL, DNAT, Segment, SegmentSummary


def list_segments(client: httpx.Client, project_id: str) -> list[Segment]:
    resp = client.get(f"/api/segment/project/{project_id}/all/")
    resp.raise_for_status()
    return [Segment.model_validate(item) for item in resp.json()]


def get_segment_summary(client: httpx.Client, segment_id: str) -> SegmentSummary:
    resp = client.get(f"/api/segment/{segment_id}/summary/")
    resp.raise_for_status()
    return SegmentSummary.model_validate(resp.json())


def list_acls(client: httpx.Client, segment_id: str) -> list[ACL]:
    resp = client.get(
        f"/api/acl/segment/{segment_id}/", params={"page_size": 1000}
    )
    resp.raise_for_status()
    return [ACL.model_validate(item) for item in resp.json()["results"]]


def list_dnats(client: httpx.Client, project_id: str) -> list[DNAT]:
    resp = client.get(
        f"/api/dnat/project/{project_id}/", params={"page_size": 1000}
    )
    resp.raise_for_status()
    return [DNAT.model_validate(item) for item in resp.json()["results"]]
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_networks.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/api/endpoints/networks.py tests/test_api/test_endpoints/test_networks.py
git commit -m "feat: ネットワークAPIエンドポイント（segments, acl, dnat）を追加"
```

---

### Task 10: タスクAPIエンドポイント（ポーリング含む）

**Files:**
- Create: `src/mdx_cli/api/endpoints/tasks.py`
- Create: `tests/test_api/test_endpoints/test_tasks.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_api/test_endpoints/test_tasks.py`:
```python
import httpx
import respx

from mdx_cli.api.endpoints.tasks import get_task, wait_for_task
from mdx_cli.models.task import Task


@respx.mock
def test_get_task():
    respx.get("/api/task/task-1/").mock(
        return_value=httpx.Response(
            200,
            json={
                "uuid": "task-1",
                "type": "デプロイ",
                "object_uuid": "vm-1",
                "object_name": "test-vm",
                "start_datetime": "2026-04-14 16:34:13",
                "end_datetime": None,
                "status": "Running",
                "progress": 50,
            },
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    task = get_task(client, "task-1")
    assert task.uuid == "task-1"
    assert task.progress == 50


@respx.mock
def test_wait_for_task_already_completed():
    respx.get("/api/task/task-1/").mock(
        return_value=httpx.Response(
            200,
            json={
                "uuid": "task-1",
                "type": "デプロイ",
                "object_uuid": "vm-1",
                "object_name": "test-vm",
                "start_datetime": "2026-04-14 16:34:13",
                "end_datetime": "2026-04-14 16:40:00",
                "status": "Completed",
                "progress": 100,
            },
        )
    )
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    task = wait_for_task(client, "task-1", poll_interval=0, timeout=5)
    assert task.status.value == "Completed"


@respx.mock
def test_wait_for_task_polls_until_done():
    route = respx.get("/api/task/task-1/")
    route.side_effect = [
        httpx.Response(
            200,
            json={
                "uuid": "task-1",
                "type": "デプロイ",
                "object_uuid": "vm-1",
                "object_name": "test-vm",
                "start_datetime": "2026-04-14 16:34:13",
                "end_datetime": None,
                "status": "Running",
                "progress": 50,
            },
        ),
        httpx.Response(
            200,
            json={
                "uuid": "task-1",
                "type": "デプロイ",
                "object_uuid": "vm-1",
                "object_name": "test-vm",
                "start_datetime": "2026-04-14 16:34:13",
                "end_datetime": "2026-04-14 16:40:00",
                "status": "Completed",
                "progress": 100,
            },
        ),
    ]
    client = httpx.Client(base_url="https://oprpl.mdx.jp")
    task = wait_for_task(client, "task-1", poll_interval=0, timeout=5)
    assert task.status.value == "Completed"
    assert route.call_count == 2
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_tasks.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: タスクエンドポイントを実装**

`src/mdx_cli/api/endpoints/tasks.py`:
```python
import time

import httpx

from mdx_cli.models.task import Task


def get_task(client: httpx.Client, task_id: str) -> Task:
    resp = client.get(f"/api/task/{task_id}/")
    resp.raise_for_status()
    return Task.model_validate(resp.json())


def wait_for_task(
    client: httpx.Client,
    task_id: str,
    poll_interval: int = 3,
    timeout: int = 600,
) -> Task:
    start = time.monotonic()
    while True:
        task = get_task(client, task_id)
        if task.is_terminal:
            return task
        elapsed = time.monotonic() - start
        if elapsed >= timeout:
            raise TimeoutError(
                f"タスク {task_id} がタイムアウトしました（{timeout}秒）"
            )
        time.sleep(poll_interval)
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_api/test_endpoints/test_tasks.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/api/endpoints/tasks.py tests/test_api/test_endpoints/test_tasks.py
git commit -m "feat: タスクAPIエンドポイント（取得・ポーリング待機）を追加"
```

---

### Task 11: 認証コマンド（auth login / logout / status）

**Files:**
- Create: `src/mdx_cli/commands/__init__.py`
- Create: `src/mdx_cli/commands/auth.py`
- Create: `tests/test_commands/__init__.py`
- Create: `tests/test_commands/test_auth.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_commands/__init__.py`:
```python
```

`tests/test_commands/test_auth.py`:
```python
from typer.testing import CliRunner
from unittest.mock import patch, MagicMock

from mdx_cli.commands.auth import app

runner = CliRunner()


def test_auth_status_not_logged_in(tmp_path, monkeypatch):
    monkeypatch.setenv("MDX_CONFIG_DIR", str(tmp_path))
    with patch("mdx_cli.commands.auth.CredentialStore") as MockStore:
        store = MockStore.return_value
        store.load_token.return_value = None
        result = runner.invoke(app, ["status"])
        assert result.exit_code == 0
        assert "ログインしていません" in result.output


def test_auth_status_logged_in(tmp_path, monkeypatch):
    monkeypatch.setenv("MDX_CONFIG_DIR", str(tmp_path))
    with patch("mdx_cli.commands.auth.CredentialStore") as MockStore:
        store = MockStore.return_value
        store.load_token.return_value = "some-jwt-token"
        store.load_credentials.return_value = ("testuser", "testpass")
        result = runner.invoke(app, ["status"])
        assert result.exit_code == 0
        assert "ログイン済み" in result.output


def test_auth_logout(tmp_path, monkeypatch):
    monkeypatch.setenv("MDX_CONFIG_DIR", str(tmp_path))
    with patch("mdx_cli.commands.auth.CredentialStore") as MockStore:
        store = MockStore.return_value
        result = runner.invoke(app, ["logout"])
        assert result.exit_code == 0
        store.delete_token.assert_called_once()
        store.delete_credentials.assert_called_once()
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_auth.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: 認証コマンドを実装**

`src/mdx_cli/commands/__init__.py`:
```python
"""CLIコマンド"""
```

`src/mdx_cli/commands/auth.py`:
```python
import typer
from rich.console import Console

from mdx_cli.credentials.store import CredentialStore
from mdx_cli.settings import Settings

app = typer.Typer(help="認証管理")
console = Console()


def _get_store() -> CredentialStore:
    settings = Settings()
    return CredentialStore(config_dir=settings.config_dir)


@app.command()
def login() -> None:
    """MDX にログインする"""
    store = _get_store()

    username = typer.prompt("ユーザー名")
    password = typer.prompt("パスワード", hide_input=True)
    otp = typer.prompt("OTP（ワンタイムパスワード）")

    store.save_credentials(username, password)

    # TODO: MDX認証基盤トークン取得API（未調査）
    # 1. username + password + otp → MDX認証基盤トークン
    # 2. POST /api/auth/ {token: <MDX認証基盤トークン>} → JWT
    # 現時点ではプレースホルダー。APIが判明したら実装する。

    console.print("[yellow]注意: ログインAPI未実装。API詳細の調査が必要です。[/yellow]")
    console.print(f"クレデンシャルを保存しました（ユーザー: {username}）")


@app.command()
def logout() -> None:
    """ログアウトしてクレデンシャルを削除する"""
    store = _get_store()
    store.delete_token()
    store.delete_credentials()
    console.print("ログアウトしました")


@app.command()
def status() -> None:
    """認証状態を確認する"""
    store = _get_store()
    token = store.load_token()
    if token:
        creds = store.load_credentials()
        username = creds[0] if creds else "不明"
        console.print(f"ログイン済み（ユーザー: {username}）")
    else:
        console.print("ログインしていません")
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_auth.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/commands/ tests/test_commands/
git commit -m "feat: 認証コマンド（login, logout, status）を追加"
```

---

### Task 12: プロジェクトコマンド

**Files:**
- Create: `src/mdx_cli/commands/project.py`
- Create: `tests/test_commands/test_project.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_commands/test_project.py`:
```python
import json
from unittest.mock import patch, MagicMock

from typer.testing import CliRunner

from mdx_cli.commands.project import app
from mdx_cli.models.project import Project

runner = CliRunner()


def test_project_list_json():
    mock_projects = [
        Project(uuid="proj-1", name="Project 1", description="desc1"),
        Project(uuid="proj-2", name="Project 2", description="desc2"),
    ]
    with patch("mdx_cli.commands.project.list_projects", return_value=mock_projects):
        with patch("mdx_cli.commands.project._get_client"):
            result = runner.invoke(app, ["list", "--json"])
            assert result.exit_code == 0
            data = json.loads(result.output)
            assert len(data) == 2
            assert data[0]["uuid"] == "proj-1"


def test_project_list_table():
    mock_projects = [
        Project(uuid="proj-1", name="Project 1", description="desc1"),
    ]
    with patch("mdx_cli.commands.project.list_projects", return_value=mock_projects):
        with patch("mdx_cli.commands.project._get_client"):
            result = runner.invoke(app, ["list"])
            assert result.exit_code == 0
            assert "proj-1" in result.output
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_project.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: プロジェクトコマンドを実装**

`src/mdx_cli/commands/project.py`:
```python
import typer

from mdx_cli.api.client import create_client
from mdx_cli.api.endpoints.projects import (
    get_project_summary,
    list_access_keys,
    list_projects,
    get_project_storage,
)
from mdx_cli.credentials.store import CredentialStore
from mdx_cli.output.formatting import render
from mdx_cli.output.tables import ACCESS_KEY_COLUMNS, PROJECT_COLUMNS
from mdx_cli.settings import Settings

app = typer.Typer(help="プロジェクト管理")


def _get_client():
    settings = Settings()
    store = CredentialStore(config_dir=settings.config_dir)
    token = store.load_token()
    return create_client(token=token)


@app.command("list")
def list_cmd(
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """アサイン済みプロジェクト一覧"""
    client = _get_client()
    projects = list_projects(client)
    render(projects, PROJECT_COLUMNS, json_mode=json)


@app.command()
def show(
    project_id: str = typer.Argument(help="プロジェクトID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """プロジェクトサマリー"""
    client = _get_client()
    summary = get_project_summary(client, project_id)
    render(summary, PROJECT_COLUMNS, json_mode=json)


@app.command()
def storage(
    project_id: str = typer.Argument(help="プロジェクトID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """ストレージ情報"""
    client = _get_client()
    info = get_project_storage(client, project_id)
    render(info, [("UUID", "uuid"), ("名前", "name")], json_mode=json)


@app.command()
def keys(
    project_id: str = typer.Argument(help="プロジェクトID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """アクセスキー一覧"""
    client = _get_client()
    access_keys = list_access_keys(client, project_id)
    render(access_keys, ACCESS_KEY_COLUMNS, json_mode=json)
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_project.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/commands/project.py tests/test_commands/test_project.py
git commit -m "feat: プロジェクトコマンド（list, show, storage, keys）を追加"
```

---

### Task 13: VMコマンド

**Files:**
- Create: `src/mdx_cli/commands/vm.py`
- Create: `tests/test_commands/test_vm.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_commands/test_vm.py`:
```python
import json
from unittest.mock import patch

from typer.testing import CliRunner

from mdx_cli.commands.vm import app
from mdx_cli.models.vm import VM

runner = CliRunner()


def _make_vm():
    return VM(
        uuid="vm-1",
        name="test-vm",
        status="Running",
        project="proj-1",
        pack_type="cpu",
        pack_num=3,
        service_level="spot",
    )


def test_vm_list_json():
    with patch("mdx_cli.commands.vm.list_vms", return_value=[_make_vm()]):
        with patch("mdx_cli.commands.vm._get_client"):
            result = runner.invoke(app, ["list", "--project-id", "proj-1", "--json"])
            assert result.exit_code == 0
            data = json.loads(result.output)
            assert len(data) == 1
            assert data[0]["uuid"] == "vm-1"


def test_vm_show_json():
    with patch("mdx_cli.commands.vm.get_vm", return_value=_make_vm()):
        with patch("mdx_cli.commands.vm._get_client"):
            result = runner.invoke(app, ["show", "vm-1", "--json"])
            assert result.exit_code == 0
            data = json.loads(result.output)
            assert data["uuid"] == "vm-1"


def test_vm_stop():
    with patch("mdx_cli.commands.vm.power_off_vm") as mock_off:
        with patch("mdx_cli.commands.vm._get_client"):
            result = runner.invoke(app, ["stop", "vm-1"])
            assert result.exit_code == 0
            mock_off.assert_called_once()
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_vm.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: VMコマンドを実装**

`src/mdx_cli/commands/vm.py`:
```python
import typer
from rich.console import Console

from mdx_cli.api.client import create_client
from mdx_cli.api.endpoints.tasks import wait_for_task
from mdx_cli.api.endpoints.vms import (
    deploy_vm,
    destroy_vm,
    get_vm,
    list_vms,
    power_off_vm,
    power_on_vm,
    sync_vms,
)
from mdx_cli.credentials.store import CredentialStore
from mdx_cli.models.vm import VMDeployRequest
from mdx_cli.output.formatting import render
from mdx_cli.output.tables import VM_COLUMNS
from mdx_cli.settings import Settings

app = typer.Typer(help="仮想マシン管理")
console = Console()


def _get_client():
    settings = Settings()
    store = CredentialStore(config_dir=settings.config_dir)
    token = store.load_token()
    return create_client(token=token)


@app.command("list")
def list_cmd(
    project_id: str = typer.Option(..., "--project-id", "-p", help="プロジェクトID", envvar="MDX_PROJECT_ID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """VM一覧"""
    client = _get_client()
    vms = list_vms(client, project_id)
    render(vms, VM_COLUMNS, json_mode=json)


@app.command()
def show(
    vm_id: str = typer.Argument(help="VM ID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """VM詳細"""
    client = _get_client()
    vm = get_vm(client, vm_id)
    render(vm, VM_COLUMNS, json_mode=json)


@app.command()
def deploy(
    project_id: str = typer.Option(..., "--project-id", "-p", help="プロジェクトID", envvar="MDX_PROJECT_ID"),
    no_wait: bool = typer.Option(False, "--no-wait", help="タスク完了を待たない"),
) -> None:
    """VMデプロイ（対話的パラメータ入力）"""
    catalog = typer.prompt("テンプレートUUID")
    vm_name = typer.prompt("VM名")
    template_name = typer.prompt("テンプレート名")
    shared_key = typer.prompt("SSH公開鍵")
    disk_size = typer.prompt("ディスクサイズ(GB)", default=40, type=int)
    pack_num = typer.prompt("パック数", default=3, type=int)
    service_level = typer.prompt("サービスレベル (spot/guarantee)", default="spot")
    segment = typer.prompt("セグメントUUID")

    req = VMDeployRequest(
        catalog=catalog,
        project=project_id,
        vm_name=vm_name,
        disk_size=disk_size,
        pack_num=pack_num,
        service_level=service_level,
        network_adapters=[{"adapter_number": 1, "segment": segment}],
        shared_key=shared_key,
        template_name=template_name,
    )

    client = _get_client()
    resp = deploy_vm(client, req)
    task_id = resp.task_id[0]
    console.print(f"デプロイタスク開始: {task_id}")

    if not no_wait:
        settings = Settings()
        task = wait_for_task(client, task_id, poll_interval=settings.task_poll_interval, timeout=settings.task_poll_timeout)
        console.print(f"タスク完了: {task.status.value}")


@app.command()
def start(
    vm_id: str = typer.Argument(help="VM ID"),
    service_level: str = typer.Option("spot", "--service-level", "-s", help="サービスレベル"),
) -> None:
    """VM起動"""
    client = _get_client()
    power_on_vm(client, vm_id, service_level)
    console.print(f"VM {vm_id} を起動しました（{service_level}）")


@app.command()
def stop(
    vm_id: str = typer.Argument(help="VM ID"),
) -> None:
    """VM停止"""
    client = _get_client()
    power_off_vm(client, vm_id)
    console.print(f"VM {vm_id} を停止しました")


@app.command()
def destroy(
    vm_id: str = typer.Argument(help="VM ID"),
    no_wait: bool = typer.Option(False, "--no-wait", help="タスク完了を待たない"),
) -> None:
    """VM削除"""
    confirm = typer.confirm(f"VM {vm_id} を削除しますか？")
    if not confirm:
        raise typer.Abort()

    client = _get_client()
    resp = destroy_vm(client, vm_id)
    task_id = resp.task_id[0]
    console.print(f"削除タスク開始: {task_id}")

    if not no_wait:
        settings = Settings()
        task = wait_for_task(client, task_id, poll_interval=settings.task_poll_interval, timeout=settings.task_poll_timeout)
        console.print(f"タスク完了: {task.status.value}")


@app.command()
def sync(
    project_id: str = typer.Option(..., "--project-id", "-p", help="プロジェクトID", envvar="MDX_PROJECT_ID"),
) -> None:
    """VM情報同期"""
    client = _get_client()
    sync_vms(client, project_id)
    console.print("VM情報を同期しました")
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_vm.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/commands/vm.py tests/test_commands/test_vm.py
git commit -m "feat: VMコマンド（list, show, deploy, start, stop, destroy, sync）を追加"
```

---

### Task 14: ネットワーク・テンプレートコマンド

**Files:**
- Create: `src/mdx_cli/commands/network.py`
- Create: `src/mdx_cli/commands/template.py`
- Create: `tests/test_commands/test_network.py`
- Create: `tests/test_commands/test_template.py`

- [ ] **Step 1: ネットワークコマンドの失敗するテストを書く**

`tests/test_commands/test_network.py`:
```python
import json
from unittest.mock import patch

from typer.testing import CliRunner

from mdx_cli.commands.network import app
from mdx_cli.models.network import Segment, DNAT

runner = CliRunner()


def test_segment_list_json():
    segments = [Segment(uuid="seg-1", name="Segment 1")]
    with patch("mdx_cli.commands.network.list_segments", return_value=segments):
        with patch("mdx_cli.commands.network._get_client"):
            result = runner.invoke(app, ["segment", "list", "--project-id", "proj-1", "--json"])
            assert result.exit_code == 0
            data = json.loads(result.output)
            assert len(data) == 1


def test_dnat_list_json():
    dnats = [DNAT(uuid="dnat-1")]
    with patch("mdx_cli.commands.network.list_dnats", return_value=dnats):
        with patch("mdx_cli.commands.network._get_client"):
            result = runner.invoke(app, ["dnat", "list", "--project-id", "proj-1", "--json"])
            assert result.exit_code == 0
            data = json.loads(result.output)
            assert len(data) == 1
```

- [ ] **Step 2: テンプレートコマンドの失敗するテストを書く**

`tests/test_commands/test_template.py`:
```python
import json
from unittest.mock import patch

from typer.testing import CliRunner

from mdx_cli.commands.template import app

runner = CliRunner()


def test_template_list_json():
    # テンプレートモデルはAPIレスポンスに合わせて調整するため、
    # 現時点ではraw dictをPydanticモデルとしてテスト
    from mdx_cli.models.network import Segment  # 仮のモデル

    templates = [Segment(uuid="tmpl-1", name="Ubuntu 22.04")]
    with patch("mdx_cli.commands.template.list_templates", return_value=templates):
        with patch("mdx_cli.commands.template._get_client"):
            result = runner.invoke(app, ["list", "--project-id", "proj-1", "--json"])
            assert result.exit_code == 0
            data = json.loads(result.output)
            assert len(data) == 1
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_network.py tests/test_commands/test_template.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 4: ネットワークコマンドを実装**

`src/mdx_cli/commands/network.py`:
```python
import typer

from mdx_cli.api.client import create_client
from mdx_cli.api.endpoints.networks import (
    get_segment_summary,
    list_acls,
    list_dnats,
    list_segments,
)
from mdx_cli.credentials.store import CredentialStore
from mdx_cli.output.formatting import render
from mdx_cli.output.tables import ACL_COLUMNS, DNAT_COLUMNS, SEGMENT_COLUMNS
from mdx_cli.settings import Settings

app = typer.Typer(help="ネットワーク管理")
segment_app = typer.Typer(help="セグメント管理")
acl_app = typer.Typer(help="ACL管理")
dnat_app = typer.Typer(help="DNAT管理")
app.add_typer(segment_app, name="segment")
app.add_typer(acl_app, name="acl")
app.add_typer(dnat_app, name="dnat")


def _get_client():
    settings = Settings()
    store = CredentialStore(config_dir=settings.config_dir)
    token = store.load_token()
    return create_client(token=token)


@segment_app.command("list")
def segment_list(
    project_id: str = typer.Option(..., "--project-id", "-p", help="プロジェクトID", envvar="MDX_PROJECT_ID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """セグメント一覧"""
    client = _get_client()
    segments = list_segments(client, project_id)
    render(segments, SEGMENT_COLUMNS, json_mode=json)


@segment_app.command("show")
def segment_show(
    segment_id: str = typer.Argument(help="セグメントID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """セグメントサマリー"""
    client = _get_client()
    summary = get_segment_summary(client, segment_id)
    render(summary, SEGMENT_COLUMNS, json_mode=json)


@acl_app.command("list")
def acl_list(
    segment_id: str = typer.Argument(help="セグメントID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """ACL一覧"""
    client = _get_client()
    acls = list_acls(client, segment_id)
    render(acls, ACL_COLUMNS, json_mode=json)


@dnat_app.command("list")
def dnat_list(
    project_id: str = typer.Option(..., "--project-id", "-p", help="プロジェクトID", envvar="MDX_PROJECT_ID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """DNAT一覧"""
    client = _get_client()
    dnats = list_dnats(client, project_id)
    render(dnats, DNAT_COLUMNS, json_mode=json)
```

- [ ] **Step 5: テンプレートコマンドとエンドポイントを実装**

`src/mdx_cli/api/endpoints/templates.py`:
```python
import httpx

from mdx_cli.models.network import Segment


# テンプレートモデルは実際のAPIレスポンスに合わせて調整する。
# 現時点ではSegmentモデル（uuid + name）を仮利用。
Template = Segment


def list_templates(client: httpx.Client, project_id: str) -> list[Template]:
    resp = client.get(
        f"/api/catalog/project/{project_id}/", params={"page_size": 1000}
    )
    resp.raise_for_status()
    return [Template.model_validate(item) for item in resp.json()["results"]]
```

`src/mdx_cli/commands/template.py`:
```python
import typer

from mdx_cli.api.client import create_client
from mdx_cli.api.endpoints.templates import list_templates
from mdx_cli.credentials.store import CredentialStore
from mdx_cli.output.formatting import render
from mdx_cli.output.tables import TEMPLATE_COLUMNS
from mdx_cli.settings import Settings

app = typer.Typer(help="テンプレート管理")


def _get_client():
    settings = Settings()
    store = CredentialStore(config_dir=settings.config_dir)
    token = store.load_token()
    return create_client(token=token)


@app.command("list")
def list_cmd(
    project_id: str = typer.Option(..., "--project-id", "-p", help="プロジェクトID", envvar="MDX_PROJECT_ID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """テンプレート一覧"""
    client = _get_client()
    templates = list_templates(client, project_id)
    render(templates, TEMPLATE_COLUMNS, json_mode=json)
```

- [ ] **Step 6: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_network.py tests/test_commands/test_template.py -v`
Expected: 全テスト PASS

- [ ] **Step 7: コミット**

```bash
git add src/mdx_cli/commands/network.py src/mdx_cli/commands/template.py src/mdx_cli/api/endpoints/templates.py tests/test_commands/test_network.py tests/test_commands/test_template.py
git commit -m "feat: ネットワーク・テンプレートコマンドを追加"
```

---

### Task 15: タスクコマンド

**Files:**
- Create: `src/mdx_cli/commands/task.py`
- Create: `tests/test_commands/test_task.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_commands/test_task.py`:
```python
import json
from unittest.mock import patch

from typer.testing import CliRunner

from mdx_cli.commands.task import app
from mdx_cli.models.task import Task

runner = CliRunner()


def _make_task():
    return Task(
        uuid="task-1",
        type="デプロイ",
        object_uuid="vm-1",
        object_name="test-vm",
        start_datetime="2026-04-14 16:34:13",
        end_datetime=None,
        status="Running",
        progress=50,
    )


def test_task_status_json():
    with patch("mdx_cli.commands.task.get_task", return_value=_make_task()):
        with patch("mdx_cli.commands.task._get_client"):
            result = runner.invoke(app, ["status", "task-1", "--json"])
            assert result.exit_code == 0
            data = json.loads(result.output)
            assert data["uuid"] == "task-1"
            assert data["progress"] == 50
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_task.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: タスクコマンドを実装**

`src/mdx_cli/commands/task.py`:
```python
import typer
from rich.console import Console

from mdx_cli.api.client import create_client
from mdx_cli.api.endpoints.tasks import get_task, wait_for_task
from mdx_cli.credentials.store import CredentialStore
from mdx_cli.output.formatting import render
from mdx_cli.output.tables import TASK_COLUMNS
from mdx_cli.settings import Settings

app = typer.Typer(help="タスク管理")
console = Console()


def _get_client():
    settings = Settings()
    store = CredentialStore(config_dir=settings.config_dir)
    token = store.load_token()
    return create_client(token=token)


@app.command()
def status(
    task_id: str = typer.Argument(help="タスクID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """タスク状態確認"""
    client = _get_client()
    task = get_task(client, task_id)
    render(task, TASK_COLUMNS, json_mode=json)


@app.command()
def wait(
    task_id: str = typer.Argument(help="タスクID"),
    json: bool = typer.Option(False, "--json", help="JSON出力"),
) -> None:
    """タスク完了まで待機"""
    client = _get_client()
    settings = Settings()
    console.print(f"タスク {task_id} の完了を待機中...")
    task = wait_for_task(
        client,
        task_id,
        poll_interval=settings.task_poll_interval,
        timeout=settings.task_poll_timeout,
    )
    render(task, TASK_COLUMNS, json_mode=json)
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_commands/test_task.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: コミット**

```bash
git add src/mdx_cli/commands/task.py tests/test_commands/test_task.py
git commit -m "feat: タスクコマンド（status, wait）を追加"
```

---

### Task 16: メインアプリ（Typerルート + 全コマンド結合）

**Files:**
- Create: `src/mdx_cli/main.py`
- Create: `tests/test_main.py`

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_main.py`:
```python
from typer.testing import CliRunner

from mdx_cli.main import app

runner = CliRunner()


def test_help():
    result = runner.invoke(app, ["--help"])
    assert result.exit_code == 0
    assert "auth" in result.output
    assert "project" in result.output
    assert "vm" in result.output
    assert "network" in result.output
    assert "template" in result.output
    assert "task" in result.output


def test_auth_help():
    result = runner.invoke(app, ["auth", "--help"])
    assert result.exit_code == 0
    assert "login" in result.output
    assert "logout" in result.output
    assert "status" in result.output


def test_vm_help():
    result = runner.invoke(app, ["vm", "--help"])
    assert result.exit_code == 0
    assert "list" in result.output
    assert "deploy" in result.output
    assert "start" in result.output
    assert "stop" in result.output
    assert "destroy" in result.output
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_main.py -v`
Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: メインアプリを実装**

`src/mdx_cli/main.py`:
```python
import typer

from mdx_cli.commands.auth import app as auth_app
from mdx_cli.commands.network import app as network_app
from mdx_cli.commands.project import app as project_app
from mdx_cli.commands.task import app as task_app
from mdx_cli.commands.template import app as template_app
from mdx_cli.commands.vm import app as vm_app

app = typer.Typer(help="MDX 1 CLI ツール")

app.add_typer(auth_app, name="auth")
app.add_typer(project_app, name="project")
app.add_typer(vm_app, name="vm")
app.add_typer(network_app, name="network")
app.add_typer(template_app, name="template")
app.add_typer(task_app, name="task")

if __name__ == "__main__":
    app()
```

- [ ] **Step 4: テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest tests/test_main.py -v`
Expected: 全テスト PASS

- [ ] **Step 5: CLIが動作することを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run mdx --help`
Expected: ヘルプテキストが表示され、全サブコマンド（auth, project, vm, network, template, task）がリストされる

- [ ] **Step 6: 全テストが通ることを確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest -v`
Expected: 全テスト PASS

- [ ] **Step 7: コミット**

```bash
git add src/mdx_cli/main.py tests/test_main.py
git commit -m "feat: メインTyperアプリ、全コマンドを結合"
```

---

### Task 17: .gitignore と最終整理

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: .gitignoreを作成**

`.gitignore`:
```
__pycache__/
*.py[cod]
*$py.class
.venv/
dist/
build/
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.idea/
.vscode/
.DS_Store
*.enc
token.json
```

- [ ] **Step 2: 全テストが通ることを最終確認**

Run: `cd /Users/aida/projects/mdx-cli && uv run pytest -v`
Expected: 全テスト PASS

- [ ] **Step 3: コミット**

```bash
git add .gitignore
git commit -m "chore: .gitignoreを追加"
```

---

## 検証方法

1. **ユニットテスト**: `uv run pytest -v` — 全テストがPASS
2. **CLIヘルプ**: `uv run mdx --help` — 全サブコマンドが表示
3. **サブコマンドヘルプ**: `uv run mdx vm --help` 等 — 各コマンドのヘルプが表示
4. **認証テスト**: `uv run mdx auth login` — プロンプトが表示される（実際のAPI呼び出しは未実装）
5. **JSON出力**: `uv run mdx project list --json` — JSON形式で出力（要ログイン）

## 未実装事項（今後のタスク）

1. **ログインAPI**: MDX認証基盤トークン取得APIの調査・実装
2. **グローバルIP管理**: IP割当・DNAT作成/削除APIの調査・実装
3. **Pydanticモデルフィールド調整**: 実際のAPIレスポンスに合わせた修正
4. **Richプログレスバー**: タスクポーリングでのプログレスバー表示
